# torch>=2.14.0 y numpy>=2.5.2 ya estan instalados en el sistema (Python 3.14)
# Se listan como referencia; no se reinstalan.
# torch==2.14.0+cpu
# numpy==2.5.2

tokenizers>=0.20
tqdm>=4.66
pytest>=8.0
psutil>=6.0

# Ingesta de datos (src/data/ingest.py): descarga directa de parquet con
# requests y lectura por row-groups con pyarrow. `datasets` ya no ejecuta
# scripts de carga, por lo que no se usa en la ingesta.
pyarrow>=17.0
requests>=2.32
datasets>=3.0
